# @visual-guard/plugin-ai

AI 差异分析插件包（规划中）。

## 目标能力

- 截图差异解释
- DOM 变化摘要
- 变化分类（样式 / 文案 / 布局 / 资源）
- 修复建议生成

## 设计原则

- 只分析失败或 changed 场景，避免成本失控
- 消费 `DiffManifest`，不侵入 core
- 图片和 DOM 需压缩后再交给模型

当前包仍为骨架，后续在插件 API 稳定后实现。
